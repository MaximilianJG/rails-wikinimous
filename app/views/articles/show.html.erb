<h1><%= @article.title %></h1>

<p><%= @article.content %></p>

<%= link_to 'Back to all articles', articles_path %>

<%= link_to 'Edit this article', edit_article_path(@article)%>

<%= link_to "Destroy this article", article_path(@article), method: :delete, data: { confirm: "Are you sure you want to delete this article?" } %>
